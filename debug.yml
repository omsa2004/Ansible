- name: Print ansible_become_password
  debug:
    var: 
      ansible_ssh_pass: "{{ ansible_ssh_pass }}"

- name: Ensure ansible_become_password is defined
  fail:
    msg: "ansible_become_password is not defined"
  when: ansible_become_password is not defined
