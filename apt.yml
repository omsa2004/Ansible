- hosts: "*"
  become: yes
  gather_facts: yes  # Python required for apt module
  vars:
    playbook_name: "apt.yml"  # Hardcoded playbook name
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: yes
        cache_valid_time: 3600
      register: update_result
      notify: send_notification

    - name: Upgrade all packages (normal upgrade)
      ansible.builtin.apt:
        upgrade: yes
        autoremove: yes
      register: upgrade_result
      notify: send_notification

    - name: Remove unnecessary packages
      ansible.builtin.apt:
        autoremove: yes
      register: cleanup_result
      notify: send_notification

  handlers:
    - name: send_notification
      ansible.builtin.uri:
        url: https://api.pushover.net/1/messages.json
        method: POST
        body:
          token: "{{ pover_token }}"  # Application token
          user: "{{ pover_id }}"      # User key
          message: >-
            Playbook {{ playbook_name }} finished.
            {% if update_result.changed or upgrade_result.changed or cleanup_result.changed %}
              Changes were made.
            {% else %}
              No changes were needed.
            {% endif %}
        body_format: form-urlencoded
